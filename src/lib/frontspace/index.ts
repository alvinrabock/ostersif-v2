/**
 * Frontspace CMS
 * Export all Frontspace utilities and types
 */

export * from './client';
export * from './types';

// Re-export the main client as default
export { frontspace as default } from './client';
