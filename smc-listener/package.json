{
  "name": "smc-sqs-listener",
  "version": "2.0.0",
  "description": "AWS SQS listener for SMC Push API match updates - triggers Next.js cache revalidation",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.700.0"
  }
}
